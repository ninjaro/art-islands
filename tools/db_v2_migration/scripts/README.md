# Scripts

One-off migration and inspection scripts belong here when they are not ready
to become production package code. The current implementation exposes the
usable entry points through `art-islands db-v2 ...`.
